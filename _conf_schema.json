{
  "permission": {
    "description": "命令权限：admin=仅管理员可用，everyone=所有人可用",
    "type": "string",
    "default": "admin",
    "options": ["admin", "everyone"]
  },
  "download_dir": {
    "description": "下载保存目录",
    "hint": "留空则使用 AstrBot 数据目录下的 plugin_data/jmcomic_downloader/download",
    "type": "string",
    "default": ""
  },
  "zip_dir": {
    "description": "zip 压缩包输出目录",
    "hint": "留空则使用 plugin_data/jmcomic_downloader/zip",
    "type": "string",
    "default": ""
  },
  "pdf_dir": {
    "description": "PDF 输出目录",
    "hint": "留空则使用 plugin_data/jmcomic_downloader/pdf",
    "type": "string",
    "default": ""
  },
  "pack_format": {
    "description": "发送文件格式",
    "hint": "zip=压缩包（保留目录结构）；pdf=把所有图片合成为一个 PDF，文件名与 zip 规则一致；未安装 pymupdf 时自动回退为 zip",
    "type": "string",
    "default": "zip",
    "options": ["zip", "pdf"]
  },
  "client_impl": {
    "description": "客户端实现",
    "hint": "api=APP端，不限IP兼容性好；html=网页端，效率高但限制IP地区",
    "type": "string",
    "default": "api",
    "options": ["api", "html"]
  },
  "image_decode": {
    "description": "是否还原禁漫的混淆图片",
    "type": "bool",
    "default": true
  },
  "image_suffix": {
    "description": "图片格式转换后缀",
    "hint": "例如 .jpg 或 .png，留空表示不转换",
    "type": "string",
    "default": ""
  },
  "image_threads": {
    "description": "同时下载图片的线程数",
    "type": "int",
    "default": 30
  },
  "photo_threads": {
    "description": "同时下载章节数",
    "hint": "0 表示自动（按 CPU 核数）",
    "type": "int",
    "default": 0
  },
  "retry_times": {
    "description": "请求失败重试次数",
    "type": "int",
    "default": 5
  },
  "proxy": {
    "description": "代理设置",
    "hint": "留空=系统代理；可填写 127.0.0.1:7890 等",
    "type": "string",
    "default": ""
  },
  "pack_mode": {
    "description": "打包文件处理模式",
    "hint": "csv_cache=保留打包文件并用 CSV 索引缓存（重复请求直接发送，超时自动删除）；delete_after_send=发送后立即删除本地打包文件与原图目录",
    "type": "string",
    "default": "csv_cache",
    "options": ["csv_cache", "delete_after_send"]
  },
  "zip_password": {
    "description": "打包文件密码（zip 解压 / PDF 打开，AES-256 加密）",
    "hint": "留空不加密；填写后发送的 zip 使用该密码 AES-256 加密、PDF 使用该密码作为打开密码，可降低 QQ 等平台的文件发送限制风险",
    "type": "string",
    "default": ""
  },
  "send_progress": {
    "description": "下载过程中发送进度消息",
    "type": "bool",
    "default": true
  },
  "max_concurrent": {
    "description": "每个会话同时运行的最大下载任务数",
    "type": "int",
    "default": 2
  },
  "search_max": {
    "description": "搜索结果显示的最大条数",
    "type": "int",
    "default": 5
  },
  "finish_reply": {
    "description": "下载完成后回复发送者的文案",
    "type": "string",
    "default": "你的本子下载完成，已发送给你"
  },
  "cache_ttl_hours": {
    "description": "缓存过期时间（小时）",
    "hint": "距最近一次发送超过该时长且无人再请求时，删除该本子的打包文件",
    "type": "int",
    "default": 24
  },
  "cache_cleanup_interval_minutes": {
    "description": "缓存清理检查间隔（分钟）",
    "hint": "后台定时扫描过期缓存（与缓存过期时间取较小值），最小 1 分钟",
    "type": "int",
    "default": 30
  },
  "cache_delete_raw": {
    "description": "缓存过期时是否同时删除原图目录",
    "type": "bool",
    "default": true
  },
  "cache_csv_path": {
    "description": "缓存索引 CSV 路径",
    "hint": "留空则使用 plugin_data/jmcomic_downloader/cache_index.csv",
    "type": "string",
    "default": ""
  },
  "cache_hit_reply": {
    "description": "缓存命中时的回复文案",
    "hint": "支持占位符 {id} {count} {first} {last}，分别对应车号、累计发送次数、首次下载时间、最近发送时间",
    "type": "string",
    "default": "✅ 命中缓存，直接发送: JM{id}（累计发送 {count} 次）"
  }
}
